#include "../Header.h"
#define SWAP(X,Y,T)\
T=X;\
X=Y;\
Y=T;
/* 전처리 */
/* 전처리 함수의 장점은 굉장히 빠르다는 것 */
/* 파라미터 넘기고, 함수 스택 초기화하고, 파라미터의 값을 복사하는 등 함수 호출에 따른 비용을 무시 할 수 있음 */
/* 중단점 찍기 불가능 */

#define P(X, Y) X##Y
// 함수 붙이기 ex) P(print,f("helloworld"))

#define PI 3.141592
// 상수 지정

// #if #else #elseif 도 가능

int main(){
    int v1,v2,temp;
    return  0;
}
